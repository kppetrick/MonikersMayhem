import React from "react";

export const HostControls: React.FC = () => {
  // TODO: controls for host: randomize teams, lock teams, start draft, start rounds, handle skips
  return (
    <main>
      <h1>Host Controls</h1>
      <p>TODO: host-only actions (teams, draft, rounds, skips).</p>
    </main>
  );
};

